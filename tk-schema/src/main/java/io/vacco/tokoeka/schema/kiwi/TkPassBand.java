package io.vacco.tokoeka.schema.kiwi;

public class TkPassBand {
  public int lo;
  public int hi;
  public int c; // Optional, defaults to 0 if not present
  public boolean lock; // Optional, defaults to false if not present
}
